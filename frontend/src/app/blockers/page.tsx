import { BlockersDashboard } from '@/components/blockers';

export default function BlockersPage() {
    return <BlockersDashboard />;
}

export const metadata = {
    title: 'Blockers & Unlockers | IDKit',
    description: 'Identify obstacles and the actions needed to overcome them',
};
