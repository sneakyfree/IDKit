import { ScenariosDashboard } from '@/components/scenarios';

export default function ScenariosPage() {
    return <ScenariosDashboard />;
}

export const metadata = {
    title: 'Scenario Universe | IDKit',
    description: 'AI-generated opportunities for growth, monetization, and content',
};
