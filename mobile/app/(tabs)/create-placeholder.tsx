/**
 * Create Placeholder
 *
 * This is a placeholder screen for the create tab.
 * The actual create flow opens as a modal.
 */

import { View } from 'react-native';

export default function CreatePlaceholder() {
  // This screen should never be shown
  // The create button opens a modal instead
  return <View />;
}
